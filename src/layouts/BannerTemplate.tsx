/**
 * Template page to body between header and footer.
 */

import { Template, TemplateProps } from "./Template";

export const BannerTemplate = (templateProps: TemplateProps) => {
  return <Template activeKey={templateProps.activeKey}>flsd</Template>;
};
